import React, { useContext } from 'react';
import Button from './Button';
const Page = () => {
    return (
        <>
            <Button />
            <p>This is a page for toggling the context</p>
        </>
    );
};

export default Page;
